const nomes = [ 'julia' , 'otaVIo', 'LIvia' , 'RAfaeLA']
const nomesUper = nomes.map((nome)=> nome.toUpperCase())// nesse caso nao precisamos do return para nada, podemos fazer a codificação do codigo dessa maneira que ira retornar da mesma forma
console.log(nomesUper)
// nesse codigo utilizamos a função map para alterar os valores de 'nome', modificando as strings, convertendo todas elas para maiúsculas